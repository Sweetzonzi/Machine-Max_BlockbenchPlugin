{
    "name": "machine-max-bb-plugin",
    "version": "0.1.0",
    "private": true,
    "description": "MachineMax Blockbench 插件构建工具",
    "scripts": {
        "build": "node scripts/build.js",
        "watch": "node scripts/build.js --watch"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    }
}
