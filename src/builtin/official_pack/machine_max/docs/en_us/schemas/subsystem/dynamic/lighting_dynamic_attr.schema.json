{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://machine_max.io/schemas/subsystem/dynamic/lighting_dynamic_attr.json",
  "title": "Lighting Subsystem Dynamic Attributes",
  "description": "Lighting subsystem instance attributes, including the light definition and model locator.",
  "properties": {
    "type": {
      "type": "string",
      "const": "machine_max:lighting",
      "description": "Subsystem type. Must be 'machine_max:lighting'."
    },
    "definition": {
      "type": "string",
      "description": "Resource location of the subsystem definition."
    },
    "locator": {
      "type": "string",
      "description": "Light locator name. Rendering uses this locator's position and rotation."
    }
  },
  "required": ["type", "definition", "locator"],
  "additionalProperties": false,
  "examples": [
    {
      "type": "machine_max:lighting",
      "definition": "machine_max:headlight_white",
      "locator": "Headlight_L"
    }
  ]
}
