{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://machine_max.io/schemas/subsystem/static/transmission_static_attr.json",
  "title": "Transmission System Subsystem Static Attributes",
  "description": "Static attributes of the transmission system, responsible for distributing input power to various output ends.",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference path, used for editor intelligent suggestions and validation",
      "default": "../../docs/en_us/schemas/subsystem/subsystem_static_attr.schema.json"
    },
    "type": {
      "type": "string",
      "const": "machine_max:transmission",
      "description": "Subsystem type, must be 'machine_max:transmission'."
    },
    "basic_durability": {
      "type": "number",
      "minimum": 0,
      "default": 20.0,
      "description": "Subsystem basic durability, becomes paralyzed when reduced to 0, restores functionality when repaired to 30% durability."
    },
    "pass_damage": {
      "type": "boolean",
      "default": true,
      "description": "Whether to pass damage to the subsystem holder. If true, damage received by the subsystem will be transferred to the sub-part."
    },
    "limit_damage": {
      "type": "boolean",
      "default": false,
      "description": "Whether to limit damage transfer value to the subsystem's remaining durability. For example, 40 damage with 20 remaining durability will only transfer 20 damage to the holder."
    },
    "hidden": {
      "type": "boolean",
      "default": false,
      "description": "Whether to hide the subsystem in HUD displays, commonly used for purely providing model part damage differentiation for non-functional subsystems"
    },
    "diff_lock": {
      "type": "string",
      "enum": ["true", "false", "auto", "manual"],
      "default": "auto",
      "description": "Differential lock mode. true: always locked; false: never locked; auto: automatically locked; manual: manually controlled locking."
    },
    "diff_lock_sensitivity": {
      "type": "number",
      "minimum": 0,
      "default": 1.0,
      "description": "Differential lock sensitivity."
    },
    "auto_diff_lock_threshold": {
      "type": "number",
      "minimum": 0,
      "default": 10.0,
      "description": "Automatic differential lock threshold, when the percentage difference in output end speed exceeds this value and diff_lock is auto, automatically enable the differential lock."
    },
    "diff_lock_inputs": {
      "type": "array",
      "items": { "type": "string" },
      "default": ["diff_lock_control"],
      "description": "List of manual differential lock control signal keys, in descending priority."
    },
    "sounds": {
      "type": "object",
      "properties": {
        "on_destroyed": {
          "$ref": "../../base/sound_event.schema.json",
          "description": "Sound event when the subsystem is destroyed.",
          "default": {
            "sound_id": "machine_max:empty_sound",
            "range": 16
          }
        },
        "on_activate": {
          "$ref": "../../base/sound_event.schema.json",
          "description": "Sound event when the subsystem is activated.",
          "default": {
            "sound_id": "machine_max:empty_sound",
            "range": 16
          }
        }
      },
      "additionalProperties": false,
      "description": "Basic sound configuration for the subsystem."
    }
  },
  "required": ["type"],
  "additionalProperties": false,
  "examples": [
    {
      "type": "machine_max:transmission",
      "basic_durability": 70.0,
      "diff_lock": "auto",
      "auto_diff_lock_threshold": 15.0,
      "diff_lock_sensitivity": 1.5
    }
  ]
}