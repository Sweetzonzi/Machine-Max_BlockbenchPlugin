{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://machine_max.io/schemas/subsystem/static/motorbike_controller_static_attr.json",
  "title": "Motorbike Controller Subsystem Static Attributes",
  "description": "Static attributes of the motorbike control system, responsible for handling driving inputs, steering control, etc., including motorbike-specific tilt angle limitations.",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference path, used for editor intelligent suggestions and validation",
      "default": "../../docs/en_us/schemas/subsystem/subsystem_static_attr.schema.json"
    },
    "type": {
      "type": "string",
      "const": "machine_max:motorbike_controller",
      "description": "Subsystem type, must be 'machine_max:motorbike_controller'."
    },
    "basic_durability": {
      "type": "number",
      "minimum": 0,
      "default": 20.0,
      "description": "Subsystem basic durability, becomes paralyzed when reduced to 0, restores functionality when repaired to 30% durability."
    },
    "pass_damage": {
      "type": "boolean",
      "default": true,
      "description": "Whether to pass damage to the subsystem holder. If true, damage received by the subsystem will be transferred to the sub-part."
    },
    "limit_damage": {
      "type": "boolean",
      "default": false,
      "description": "Whether to limit damage transfer value to the subsystem's remaining durability. For example, 40 damage with 20 remaining durability will only transfer 20 damage to the holder."
    },
    "hidden": {
      "type": "boolean",
      "default": false,
      "description": "Whether to hide the subsystem in HUD displays, commonly used for purely providing model part damage differentiation for non-functional subsystems"
    },
    "min_steering_radius": {
      "type": "number",
      "minimum": 0,
      "default": 3.0,
      "description": "Minimum steering radius, in meters. Used for steering limits at low speeds and when handbrake is applied."
    },
    "lateral_acceleration": {
      "oneOf": [
        {
          "type": "number",
          "minimum": 0,
          "default": 8.0,
          "description": "Lateral acceleration limit, in m/s². Used to calculate steering radius in real-time based on speed (radius = speed² / acceleration)."
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "number",
            "minimum": 0
          },
          "description": "Speed-lateral acceleration mapping table, where keys are speed (km/h) and values are corresponding lateral acceleration limits (m/s²). Supports linear interpolation.",
          "examples": [
            {
              "0": 8.0,
              "30": 6.0,
              "60": 4.0,
              "100": 3.0
            }
          ]
        }
      ],
      "default": 8.0,
      "description": "Lateral acceleration limit, in m/s². Used to calculate steering radius in real-time based on speed (radius = speed² / acceleration)."
    },
    "max_drift_angular_velocity": {
      "oneOf": [
        {
          "type": "number",
          "minimum": 0,
          "default": 57.3,
          "description": "Maximum drift angular velocity, in °/s. Used as the target value for PD controller during drifting."
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "number",
            "minimum": 0
          },
          "description": "Speed-maximum drift angular velocity mapping table, where keys are speed (km/h) and values are corresponding maximum drift angular velocity (°/s). Supports linear interpolation.",
          "examples": [
            {
              "0": 45.8,
              "30": 57.3,
              "60": 68.8,
              "100": 85.9
            }
          ]
        }
      ],
      "default": 57.3,
      "description": "Maximum drift angular velocity, in °/s. Used as the target value for PD controller during drifting."
    },
    "manual_gear_shift": {
      "type": "boolean",
      "default": false,
      "description": "Whether to enable manual gear shifting. If false, automatic shifting is used."
    },
    "auto_hand_brake": {
      "type": "boolean",
      "default": true,
      "description": "Whether to automatically apply handbrake. Automatically applies handbrake when vehicle is stationary and no input is given."
    },
    "control_inputs": {
      "type": "array",
      "items": { "type": "string" },
      "default": ["move_control"],
      "description": "List of control input signal keys, from highest to lowest priority."
    },
    "max_angle": {
      "type": "number",
      "minimum": 0,
      "maximum": 90,
      "default": 30.0,
      "description": "Tilt angle beyond which the motorbike is considered to have lost balance and will no longer apply correction forces, in degrees."
    },
    "parking_angle": {
      "type": "number",
      "minimum": -90,
      "maximum": 90,
      "default": 5.0,
      "description": "Target tilt angle when the motorbike is parked, can be positive or negative, in degrees. Positive values indicate left tilt, negative values indicate right tilt."
    },
    "correction_force_multiplier": {
      "type": "number",
      "minimum": 0.1,
      "maximum": 10.0,
      "default": 1.0,
      "description": "Correction force multiplier, used to scale the overall balance adjustment force of the motorbike control system."
    },
    "sounds": {
      "type": "object",
      "properties": {
        "on_destroyed": {
          "$ref": "../../base/sound_event.schema.json",
          "description": "Sound event when the subsystem is destroyed.",
          "default": {
            "sound_id": "machine_max:empty_sound",
            "range": 16
          }
        },
        "on_activate": {
          "$ref": "../../base/sound_event.schema.json",
          "description": "Sound event when the subsystem is activated.",
          "default": {
            "sound_id": "machine_max:empty_sound",
            "range": 16
          }
        }
      },
      "additionalProperties": false,
      "description": "Basic sound configuration for the subsystem."
    }
  },
  "required": ["type"],
  "additionalProperties": false,
  "examples": [
    {
      "type": "machine_max:motorbike_controller",
      "basic_durability": 40.0,
      "min_steering_radius": 3.0,
      "lateral_acceleration": 8.0,
      "max_drift_angular_velocity": 2.0,
      "manual_gear_shift": false,
      "control_inputs": ["move_control"],
      "max_angle": 45.0,
      "parking_angle": 5.0,
      "correction_force_multiplier": 1.2
    },
    {
      "type": "machine_max:motorbike_controller",
      "basic_durability": 35.0,
      "min_steering_radius": 2.5,
      "lateral_acceleration": {
        "0": 8.0,
        "30": 6.0,
        "60": 4.0
      },
      "max_drift_angular_velocity": {
        "0": 1.5,
        "30": 2.0,
        "60": 2.5
      },
      "manual_gear_shift": true,
      "abs_enabled": true,
      "abs_target_slip_ratio": 0.15,
      "abs_wheel_radius": 0.3,
      "max_angle": 35.0,
      "parking_angle": -3.0,
      "correction_force_multiplier": 0.8
    }
  ]
}