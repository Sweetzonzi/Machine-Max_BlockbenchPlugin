# 03 — 交互设计 (修订版)

> 最后更新：2026-05-14 | 修订：反映 Part-Variant-SubPart 三级架构 + 多零件项目

---

## 1. 设计目标

- **零命名记忆**：所有骨骼/Locator 通过可视化选择，不要求用户记忆命名规则
- **所见即所得**：3D 视口中用颜色标注当前变体的标记
- **渐进复杂度**：简单场景默认值覆盖，高级参数折叠隐藏
- **上下文驱动**：根据当前选中元素类型和层级，自动切换右侧属性面板内容
- **多零件管理**：一个模型可定义多个零件，每个零件有独立变体

---

## 2. 整体布局

进入"零件定义"模式后的界面布局：

```
┌──────────────────────────────────────────────────────────────────────┐
│ [模式栏]  Edit | Paint | Animate | Display | 📦 零件定义            │
├──────────────────────────────────────────────────────────────────────┤
│ 零件: [wine_fox_hull ▾]  变体: [left ▾]  [+ 零件]  [⚙ 项目管理]   │  ← 导航栏
├────────────────┬──────────────────────┬──────────────────────────────┤
│                │                      │  ┌─ 零件属性 (wine_fox_hull)┤
│   Outliner     │                      │  │ icon / 耐久参数          │
│   (增强版)     │      3D 视口         │  └──────────────────────────┘
│  仅高亮当前    │   (仅高亮当前变体)   │  ┌─ 变体: left ────────────┐
│  变体的标记    │                      │  │ model / textures / tags │
│                │   🔵 蓝色=子零件     │  └──────────────────────────┘
│  📦 = 子零件   │   🔴 红色=碰撞箱     │  ┌─ 子零件属性 ────────────┐
│  🔵 = 碰撞箱   │   🟢 绿色=连接点     │  │ 物理/碰撞/连接点/子系统 │
│  📌 = 连接点   │   🟡 黄色=座位       │  └──────────────────────────┘
│  ⚙  = 子系统   │   🟠 橙色=灯光       │  ┌─ 选中元素详情 ──────────┐
│                │                      │  │ 碰撞箱/连接点/子系统配置 │
│                │                      │  └──────────────────────────┘
├────────────────┴──────────────────────┴──────────────────────────────┤
│ [状态栏]  零件: 11个 | 当前变体: left | 子零件: 1 | 连接点: 2       │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 3. 项目级管理

### 3.1 零件切换

顶部的导航栏是**模式内的核心控件**：

```
零件: [wine_fox_hull      ▾]  变体: [left ▾]  [+ 新建零件]  [⚙ 项目管理]
```

| 控件 | 行为 |
|------|------|
| `零件` 下拉 | 切换当前编辑的零件 → Outliner 图标、3D 标注、右侧面板全部刷新 |
| `变体` 下拉 | 切换当前编辑的变体 → 同零件内切换变体视图 |
| `[+ 新建零件]` | 弹出对话框：输入零件 ID（如 `wine_fox_gt_spoiler`）→ 创建空零件配置 |
| `[⚙ 项目管理]` | 打开项目全貌面板（见 3.3） |

### 3.2 切换流程

```
用户切换零件 "wine_fox_door"
        ↓
1. 保存当前零件 "wine_fox_hull" 的修改
2. 清除所有 Outliner 图标和 3D 标注
3. 加载 "wine_fox_door" 的 element_markers
4. 读取当前变体（上次退出时记录，或第一个变体）
5. 仅高亮 currentVariant 的标记：
   → Outliner 图标更新
   → 3D 标注创建
6. 右侧面板刷新 → 显示空闲页（零件全局属性）
        ↓
用户切换变体 "right"
        ↓
1. 保存当前变体 "left" 的修改
2. 清除标注
3. 加载 "right" 的 element_markers
4. 刷新标注
5. 面板回到空闲页
```

### 3.3 项目管理面板

点击 `[⚙ 项目管理]` 打开全貌视图：

```
┌─ MachineMax 项目管理 ──────────────────┐
│                                        │
│ 模型: wine_fox.bbmodel                 │
│ 命名空间: [machine_max        ]        │
│ 导出目录: [D:\...\spark_modules\ 📁]   │
│                                        │
│ ── 零件列表 (11) ──────────────────    │
│                                        │
│ ┌──────┬────────────┬────────┬──────┐  │
│ │ 零件 │ 变体数     │ 子零件 │ 状态 │  │
│ ├──────┼────────────┼────────┼──────┤  │
│ │ hull │ 1(default) │ 1main  │ ✓    │  │
│ │ door │ 2(L/R)     │ 1main  │ ✓    │  │
│ │ bum..│ 2(F/B)     │ 1main  │ ✓    │  │
│ │ whe..│ 4(LF/RF..) │ 1main  │ ⚠    │  │
│ │ hood │ 1(default) │ 1main  │ ✓    │  │
│ │ ...  │            │        │      │  │
│ └──────┴────────────┴────────┴──────┘  │
│                                        │
│ [设为当前编辑] [复制零件] [删除零件]    │
│                                        │
│ ── 共享定义 ───────────────────────    │
│ 连接点定义: 12 个  [管理...]           │
│ 子系统型号: 8 个   [管理...]           │
│ 材料定义:   5 个   [管理...]           │
│                                        │
│ [关闭]                                 │
└────────────────────────────────────────┘
```

### 3.4 新建/复制零件

```
┌─ 新建零件 ─────────────────────────────┐
│                                        │
│ 零件 ID: [wine_fox_gt_spoiler  ]       │
│ (导出的 JSON 文件名)                    │
│                                        │
│ 初始变体名: [default           ]       │
│ 模型: [machine_max:wine_fox.geo]       │
│                                        │
│ [取消]                [创建]           │
└────────────────────────────────────────┘
```

---

## 4. Outliner 增强

### 4.1 图标体系

进入零件定义模式后，**仅当前零件当前变体的已标记元素**更换图标/颜色。未标记元素和不在当前变体范围内的元素保持默认外观。

| 用途 | 图标 | Font Awesome class | 颜色标记 |
|------|------|-------------------|---------|
| 未标记 (默认) | 默认图标 | 不作修改 | 灰色 |
| 子零件 Group | 📦 `fa-box` / `fa-cube` | `fa fa-cube` | 蓝色 #4A90D9 |
| 碰撞箱 Group | 🔵 `fa-shield` | `fa fa-shield` | 红色 #D94A4A |
| 连接点 Locator | 📌 `fa-plug` | `fa fa-plug` | 绿色 #4AD94A |
| 座位 Locator | 🟡 `fa-chair` | `fa fa-chair` | 黄色 #D9C94A |
| 灯光 Locator | 🟠 `fa-lightbulb` | `fa fa-lightbulb` | 橙色 #D97E4A |
| 其他子系统 Locator | ⚙ `fa-cog` | `fa fa-cog` | 紫色 #9B4AD9 |

### 4.2 右键菜单

进入模式后，Outliner 中各节点的右键菜单追加 MachineMax 专属项：

**Group（骨骼）右键菜单追加：**
```
┌──────────────────────────┐
│ (原有菜单项...)          │
│ ────────────────────────│
│ 📦 标记为子零件         │  ← 新增
│ 🔵 标记为碰撞箱         │  ← 新增
│ ⚙ 添加子系统...    →   │  ← 新增（含子菜单列出18种类型）
│ ────────────────────────│
│ 🔍 在属性面板中查看     │  ← 新增
│ 🗑️ 清除 MachineMax 标记 │  ← 新增（仅已标记时显示）
└──────────────────────────┘
```

**Locator（定位器）右键菜单追加：**
```
┌──────────────────────────┐
│ (原有菜单项...)          │
│ ────────────────────────│
│ 📌 标记为连接点         │  ← 新增
│ 🟡 标记为座位定位点     │  ← 新增
│ 🟠 标记为灯光定位点     │  ← 新增
│ ⚙ 标记为子系统定位点   │  ← 新增
│ ────────────────────────│
│ 🔍 在属性面板中查看     │  ← 新增
│ 🗑️ 清除 MachineMax 标记 │  ← 新增（仅已标记时显示）
└──────────────────────────┘
```

**Cube（方块）右键菜单追加：**
```
┌──────────────────────────┐
│ (原有菜单项...)          │
│ ────────────────────────│
│ 🔵 查看所属碰撞箱配置   │  ← 新增（仅父骨骼已标记为碰撞箱时）
└──────────────────────────┘
```

### 4.3 快速筛选

Outliner 顶部增加筛选按钮栏（仅在零件定义模式下显示）：

```
[全部] [子零件] [碰撞箱] [连接点] [子系统] [未标记]
```

作用：过滤 Outliner 只显示对应用途的节点，帮助作者快速定位。

---

## 5. 3D 视口标注

### 5.1 颜色高亮体系

在 3D 视口中，**仅当前零件当前变体的已标记元素**显示颜色标注：

| 用途 | 3D 表现 | 实现方式 |
|------|---------|---------|
| 子零件骨骼 | **蓝色** 半透明 BoxHelper | THREE.BoxHelper |
| 碰撞箱骨骼 | **红色** 半透明 BoxHelper | THREE.BoxHelper |
| 碰撞箱方块 | **红色** 线框覆盖 (Wireframe) | THREE.LineSegments |
| 连接点 Locator | **绿色** Sprite + 方向箭头 | THREE.Sprite + THREE.ArrowHelper |
| 座位 Locator | **黄色** Sprite (椅子图标) | THREE.Sprite |
| 灯光 Locator | **橙色** Sprite (灯泡图标) | THREE.Sprite |
| 子系统 Locator | **紫色** Sprite (齿轮图标) | THREE.Sprite |
| 未标记骨骼 | **灰色** 半透明 BoxHelper | THREE.BoxHelper |

### 5.2 标注生命周期

- **进入模式**：遍历所有标记配置，创建对应 THREE 对象加入 `Canvas.gizmos`
- **标记变更**：即时更新（删除旧标注 + 创建新标注）
- **退出模式**：批量清除所有 gizmos
- **相机变化**：Sprite 始终面向相机（THREE.Sprite 自动处理）

### 5.3 悬停高亮

鼠标悬停在 Outliner 中的节点上时：
- 对应 3D 标注高亮（增大亮度或改变材质）
- 显示 Tooltip（用途名称 + 简要配置摘要）

---

## 6. 右侧属性面板

### 6.1 面板概述

属性面板根据**当前编辑层级**动态切换内容。层级关系：

```
零件全局 (PartType级)  →  变体属性 (VariantAttr级)  →  选中元素详情
```

用户在不同场景下看到不同页面：

**面板状态机：**

```
┌─────────┐     选中子零件Group     ┌──────────────┐
│ 无选中   │ ──────────────────→  │ 子零件属性页 │
│ (空闲页) │ ←──────────────────  │              │
└─────────┘     取消选中           └──────────────┘
     │                                        │
     │ 选中碰撞箱Group          选中连接点Locator
     ↓                                        ↓
┌──────────────┐                  ┌──────────────┐
│ 碰撞箱属性页 │                  │ 连接点属性页 │
└──────────────┘                  └──────────────┘
     │                                        │
     │ 选中子系统配置            选中子系统Locator
     ↓                                        ↓
┌──────────────┐                  ┌──────────────┐
│ 子系统属性页 │                  │ 子系统属性页 │
└──────────────┘                  └──────────────┘
```

### 6.2 空闲页（零件全局属性 + 当前变体属性）

当没有选中任何 MachineMax 标记元素时显示。分为上中下三部分：零件全局属性、当前变体属性、变体列表快览。

```
┌─ 零件: wine_fox_hull ───────────────────┐
│                                          │
│ ── 零件全局属性 ─────────────────────    │
│ 图标:     [📷 选择图标...    ]           │
│ 耐久贡献: [0.60]  (vehicle_durability)   │
│ 伤害传导: [1.00]  (vehicle_damage)       │
│ 摧毁传导: [0.10]  (destroyed)            │
│ 功能阈值: [0.30]  (functional_threshold) │
│ 共享耐久: [✓]     (share_durability)     │
│ 最大堆叠: [1   ]  (1~64)                │
│                                          │
│ ── 变体: left ───────────────────────    │
│ 模型: [machine_max:wine_fox.geo   ▾]     │
│ 动画: [machine_max:wine_fox       ▾]     │
│ 贴图: [管理贴图映射...]                 │
│ 标签: [machine_max:door] [×]            │
│       [machine_max:wine_fox] [×]        │
│       [machine_max:left] [×]            │
│       [+ 添加标签]                       │
│                                          │
│ ── 变体列表 (2) ──────────────────────   │
│ ┌────────────────────────────┐           │
│ │ left  (当前)    [编辑] [删]│           │
│ │ right           [编辑] [删]│           │
│ │ [+ 添加变体]              │           │
│ └────────────────────────────┘           │
│                                          │
│ ── 子零件 (1) ───────────────────────    │
│ sub_part.machine_max.main     [展开]     │
│                                          │
│ ── 碰撞箱 (3) ───────────────────────    │
│ LeftDoorHitBox   box   steel  3mm  [编辑]│
│ LeftDoorMirror   box   glass  1mm  [编辑]│
│ LeftFrontGlass   box   glass  1mm  [编辑]│
│ [+ 添加碰撞箱]                           │
│                                          │
│ ── 连接点 (2) ───────────────────────    │
│ connector.machine_max.axis → fixed_down  │
│ connector.machine_max.lock → fixed_down  │
│ [+ 添加连接点]                            │
│                                          │
│ ── 子系统 (2) ───────────────────────    │
│ subsystem...glass  basic  std_glass [编辑]│
│ subsystem...mirror basic  std_glass [编辑]│
│ [+ 添加子系统]                            │
└──────────────────────────────────────────┘
```

### 6.3 子零件属性页

选中已标记为子零件的 Group 时显示：

```
┌─ 子零件: main_body ──────────────────┐
│                                      │
│ 内部名称: sub_part.machine_max.main   │
│                                      │
│ ── 骨骼范围 ──────────────────────── │
│ 起始骨骼: [AllHull           ▾]      │
│ 排除骨骼:                           │
│   LeftWheel [×]  RightWheel [×]     │
│   [+ 添加排除骨骼]                   │
│                                      │
│ ── 物理属性 ──────────────────────── │
│ 耐久度: [20.0 ]                      │
│ 质量:   [25.0 ] kg                   │
│ 质心:   [mass_center         ▾]      │
│ 投影面积: X[1.5] Y[0.8] Z[3.2] m²   │
│                                      │
│ ── 地形碰撞 ──────────────────────── │
│ 碰撞模式: [true   ▾]                 │
│          true | ground | false       │
│ 碰撞高度: [-1.0 ] (仅ground模式)     │
│ 攀爬辅助: [  ] (轮子类建议开启)      │
│                                      │
│ ── 流体动力优先级 ────────────────── │
│ hydro_priority: [0  ]                │
│                                      │
│ ── 碰撞箱 (3) ───────────────────── │
│  HullHitBox     box   steel   3mm   │
│  [编辑] [删除]                      │
│  GlassBox       box   glass   1mm   │
│  [编辑] [删除]                      │
│  EngineBox      box   steel   5mm   │
│  [编辑] [删除]                      │
│  [+ 添加碰撞箱]                     │
│                                      │
│ ── 连接点 (2) ───────────────────── │
│  LeftWheel  → machine_max:wheel     │
│  [编辑] [删除]                      │
│  RightWheel → machine_max:wheel     │
│  [编辑] [删除]                      │
│  [+ 添加连接点]                     │
│                                      │
│ ── 子系统 (1) ───────────────────── │
│  engine  → machine_max:engine_v8    │
│  [编辑] [删除]                      │
│  [+ 添加子系统]                     │
└──────────────────────────────────────┘
```

### 6.4 碰撞箱属性页

```
┌─ 碰撞箱: HullHitBox ─────────────────┐
│                                      │
│ 碰撞形状ID: [part            ]       │
│                                      │
│ 类型:  [box       ▾]                │
│        box | sphere | cylinder       │
│        | capsule | wheel             │
│                                      │
│ 材质:  [machine_max:structural ▾]    │
│  [新建材质...]                       │
│                                      │
│ 厚度:  [3    ] mm                    │
│                                      │
│ 关联子系统: [无               ▾]     │
│ (碰撞箱受伤时该子系统同步受伤)        │
│                                      │
│ 条件表达式:                          │
│ [!subpart.subsystem_destroyed(...)]  │
│ (Molang, 为空则始终生效)             │
│                                      │
│ ── 材质覆写 (可选) ───────────────  │
│  [展开/折叠]                         │
│  摩擦系数: [0.50]                    │
│  弹性系数: [0.30]                    │
│  ...                                 │
│                                      │
│ 📐 骨骼方块信息 (只读):              │
│  包含方块数: 1                       │
│  尺寸: 2.0 × 1.5 × 0.5 m           │
│  位置: (0, 1.0, -2.0)              │
└──────────────────────────────────────┘
```

### 6.5 连接点属性页

```
┌─ 连接点: LeftFrontWheel ─────────────┐
│                                      │
│ 内部名称:                            │
│ connector.machine_max.left_front     │
│                                      │
│ ── 定位器 ────────────────────────  │
│ Locator: LeftFrontWheel              │
│ 位姿: (1.2, -0.5, 2.0) / (0°,0°,0°)│
│                                      │
│ ── 静态定义 ──────────────────────  │
│ 引用:  [machine_max:wheel_mount ▾]   │
│ [📝 编辑此定义] [🆕 新建定义...]    │
│                                      │
│ ── 静态属性预览 (来自定义) ──────── │
│ 类型: Simple                         │
│ 方向: yp                             │
│ 完整性: 20.0 | 减免: 2.0             │
│ 倍率: 1.5   | 吸收: 0.2             │
│                                      │
│ ── 属性覆写 (可选) ───────────────  │
│  [展开/折叠]                         │
│  integrity: [80.0]                   │
│  impact_multiplier: [0.5]            │
│  [+ 添加覆写字段]                    │
│                                      │
│ ── 信号与能量 ────────────────────  │
│ 信号目标:                            │
│  move_control → car_controller  [×] │
│  steering     → steer_knuckle   [×] │
│  [+ 添加信号目标]                    │
│                                      │
│ 信号转译:                            │
│  steer → steering               [×] │
│  [+ 添加转译规则]                    │
│                                      │
│ 机械能目标: [无            ▾]       │
│                                      │
│ 内部连接点: [  ]                     │
│ (仅同零件内子零件间自动连接)          │
└──────────────────────────────────────┘
```

### 6.6 子系统属性页（以 engine 为例）

```
┌─ 子系统: engine ──────────────────────┐
│                                       │
│ 类型:  [machine_max:engine   ▾]      │
│        (18种类型可选)                  │
│                                       │
│ 型号定义:                             │
│ [machine_max:engine_v8        ▾]     │
│ [📝 编辑此型号] [🆕 新建型号...]     │
│                                       │
│ ── 信号输出 (engine 专属) ────────── │
│ 功率输出目标: [gearbox       ▾]      │
│                                       │
│ 转速信号输出:                         │
│  engine_speed → gearbox          [×] │
│  [+ 添加转速信号输出]                 │
│                                       │
│ ── 型号属性预览 ──────────────────── │
│ 最大功率: 350.0 kW                    │
│ 最大扭矩: 500.0 N·m                   │
│ 怠速转速: 800.0 rpm                   │
│ 红线转速: 7500.0 rpm                  │
│ 峰值扭矩转速: 5200.0 rpm              │
│ 转动惯量: 10.0 kg·m²                  │
│ 四冲程: ✓                             │
│ 气缸数: 8                             │
│                                       │
│ (不同 type 显示不同属性预览)           │
└───────────────────────────────────────┘
```

### 6.7 面板中"添加/新建"子对话框

点击属性面板中的各种 `[+ 添加...]` 或 `[🆕 新建...]` 按钮时弹出的子对话框。

#### 6.7.1 新建连接点静态定义

```
┌─ 新建连接点静态定义 ──────────────────┐
│                                       │
│ 文件名: [wheel_mount          ]       │
│ 保存至: connectors/                   │
│                                       │
│ ── 基本属性 ──────────────────────    │
│ 类型:  [Simple       ▾]              │
│        Simple | Advanced              │
│                                       │
│ 方向:  [yp      ▾]                   │
│       xp|xn|yp|yn|zp|zn              │
│                                       │
│ ── 结构强度 ──────────────────────    │
│ 完整性 (integrity):     [20.0 ]       │
│ 冲击减免 (reduction):   [2.0  ]       │
│ 冲击倍率 (multiplier):  [1.5  ]       │
│ 冲击吸收 (absorption):  [0.2  ] (0~1) │
│                                       │
│ ── 碰撞选项 ──────────────────────    │
│ 零件间碰撞: [  ] (collide_between)    │
│                                       │
│ ── 标签匹配 ──────────────────────    │
│ 必需标签 (全部满足):                  │
│  [+ 添加]                             │
│ 可接受标签 (至少一个):                │
│  [+ 添加]                             │
│ 禁止标签 (全部不满足):                │
│  [+ 添加]                             │
│                                       │
│ ── 关节属性 (仅Advanced时显示) ────   │
│ [+ 添加自由度轴]                      │
│                                       │
│ [取消]              [创建并引用]      │
└───────────────────────────────────────┘
```

#### 6.7.2 新建子系统型号

子系统型号编辑对话框需要根据 `type` 动态显示字段。有以下 18 种 type，其独特的参数字段在 MachineMax wiki 5.5 中有完整的说明。

下面只展示 engine 类型的示例，其他类型类推：

```
┌─ 新建发动机型号 ──────────────────────┐
│                                       │
│ 文件名: [engine_v8            ]       │
│ 保存至: subsystems/                   │
│                                       │
│ ── 公共基础 ──────────────────────    │
│ 基础耐久: [20.0 ]                     │
│ 穿透伤害: [✓] (pass_damage)           │
│ 限制伤害: [  ] (limit_damage)         │
│ 隐藏显示: [  ] (hidden)               │
│                                       │
│ ── 音效 ──────────────────────────    │
│ 摧毁音效: [选择...]                   │
│ 激活音效: [选择...]                   │
│ 停用音效: [选择...]                   │
│                                       │
│ ── 动力参数 ──────────────────────    │
│ 最大功率 (kW): [350.0 ]              │
│ 最大扭矩 (N·m): [500.0 ]              │
│ 怠速转速 (rpm): [800.0 ]             │
│ 怠速扭矩比: [0.333] (0~1)            │
│ 峰值扭矩转速: [5200.0]               │
│ 红线转速 (rpm): [7500.0]             │
│ 红线扭矩比: [0.90 ] (0~1)            │
│                                       │
│ ── 机械属性 ──────────────────────    │
│ 转动惯量: [10.0 ] kg·m²              │
│ 四冲程: [✓]                           │
│ 气缸数: [8  ]                         │
│                                       │
│ ── 阻力与操控 ────────────────────    │
│ 阻力系数: [20.0, 0.1, 0.00005]       │
│ 控制信号频道:                         │
│  [engine_control] [×]                 │
│  [move_control  ] [×]                 │
│  [+ 添加频道]                         │
│                                       │
│ ── 音效 (高级) ───────────────────    │
│ [展开/折叠: 自定义工况音效映射]        │
│                                       │
│ [取消]              [创建并引用]      │
└───────────────────────────────────────┘
```

---

## 7. 工具栏按钮

模式工具栏（在"零件定义"模式下显示）：

| 按钮 | 图标 | 功能 |
|------|------|------|
| 导出内容包 | 💾 | 打开导出对话框 |
| 导入已有配置 | 📂 | 从已有 JSON 反解析（后续版本） |
| 自动分析模型 | 🔍 | 扫描所有 Group/Locator，根据命名启发式自动建议标记 |
| 校验配置 | ✓ | 运行完整性校验，列出缺失字段和无效引用 |
| 变体切换 | `[default ▾]` | 切换当前编辑的变体 |

---

## 8. 导出对话框

```
┌─ 导出 MachineMax 内容包 ──────────────┐
│                                       │
│ 目标目录:                             │
│ [D:\mc\spark_modules\my_pack    📁]  │
│                                       │
│ 命名空间: [machine_max]               │
│                                       │
│ ── 将生成/更新的文件 ──────────────   │
│                                       │
│ 📄 parts/my_chassis.json              │
│     ✓ 顶层字段 / ✓ 变体 default      │
│     ✓ 变体 left / ✓ 变体 right       │
│                                       │
│ 📄 connectors/wheel_mount.json        │
│     [新建]                            │
│                                       │
│ 📄 connectors/door_hinge.json         │
│     [新建]                            │
│                                       │
│ 📄 subsystems/engine_v8.json          │
│     [新建]                            │
│                                       │
│ 📄 lang/zh_cn.json                    │
│     [追加条目]                        │
│                                       │
│ 📄 meta.json                          │
│     [已存在 / 跳过]                   │
│                                       │
│ ── 全局选项 ──────────────────────    │
│ 遇到已存在文件:                       │
│  ○ 跳过  ● 覆盖  ○ 询问              │
│                                       │
│ [取消]               [导出到目录]     │
└───────────────────────────────────────┘
```

---

## 9. 校验结果展示

点击"校验"按钮或导出前自动运行：

```
┌─ 配置校验结果 ────────────────────────┐
│                                       │
│ ✓ 通过 (12)                           │
│   ✓ 零件 ID 已设置: my_chassis        │
│   ✓ 至少有一个变体: 3 个              │
│   ✓ 所有变体都有模型引用              │
│   ...                                 │
│                                       │
│ ⚠ 警告 (2)                            │
│   ⚠ 子零件 "main_body" 未设置碰撞箱   │
│   ⚠ 变体 "right" 未设置标签           │
│                                       │
│ ✕ 错误 (1)                            │
│   ✕ 连接点 "LeftFrontWheel" 引用的    │
│     Locator 在模型中不存在             │
│                                       │
│ [忽略警告，继续导出]  [返回修改]      │
└───────────────────────────────────────┘
```

---

## 10. 与原有 Blockbench 功能的协作

| 原有功能 | 在零件定义模式下的行为 |
|---------|----------------------|
| 建模工具 (move/rotate/scale) | **禁用**。3D 视口变为只读 |
| 撤销/重做 | 对 MachineMax 配置的修改应走自己的撤销栈 |
| 贴图编辑 | **不可用**。切换到 Paint 模式使用 |
| 动画编辑 | **不可用**。切换到 Animate 模式使用 |
| 文件保存 (Ctrl+S) | 保存 .bbmodel 文件（含 MachineMax 配置） |

---

## 11. 可访问性

- **键盘快捷键**：
  - `Ctrl+Shift+P` → 切换零件定义模式
  - `Ctrl+Shift+E` → 导出内容包
  - `Ctrl+Shift+V` → 校验配置
  - `Tab` → 在属性面板字段间切换
- **错误反馈**：表单字段输入无效时，字段边框变红 + Tooltip 显示错误信息
- **进度反馈**：导出时显示进度条
- **撤销支持**：配置修改支持 Ctrl+Z 撤销（独立撤销栈）
